<%= form_for [@facility, @sensor], method: :patch, html: {id: 'editSensorForm'} do |f| %>
  <%= render partial: 'points/show.html.erb', locals: { point: @sensor.point, point_type: "sensor", path: facility_sensor_path(@sensor, @facility.id) } %>
<% end %>

<%= line_chart @data, xtitle: "Time", ytitle: @unit %>
